import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jingwei.tian.pg',
  appName: 'pg-mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
