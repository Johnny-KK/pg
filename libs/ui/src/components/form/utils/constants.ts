import type { FormLayout } from './types';

// 默认表单布局
const DEFAULT_FORM_LAYOUT: FormLayout = 'RC';

// 默认标签宽度
const DEFAULT_LABEL_WIDTH = 100;

// 默认栅格化宽度为6
const DEFAULT_SPAN = 6;

// 默认是否自动根据label生成placeholder
const DEFAULT_AUTO_PLACEHOLDER = true;

// 默认是否允许清空
const DEFAULT_CLEARABLE = true;

// 默认是否是必填字段
const DEFAULT_REQUIRED = false;

export { DEFAULT_FORM_LAYOUT, DEFAULT_LABEL_WIDTH, DEFAULT_SPAN, DEFAULT_AUTO_PLACEHOLDER, DEFAULT_CLEARABLE, DEFAULT_REQUIRED };
